import React from 'react';
import PageTitle from '../../components/PageTitle/PageTitle';

const About = () => {
    return (
        <div>
            <PageTitle title={'doctors portal - about'} />
            <h2>This is About route</h2>
        </div>
    );
};

export default About;